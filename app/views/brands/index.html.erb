# encoding: utf-8
<h1>Список брэндов</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @brands.each do |brand| %>
  <tr>
    <td><%= brand.name %></td>
    <td><%= link_to 'Показать', brand %></td>
    <td><%= link_to 'Редактировать', edit_brand_path(brand) %></td>
    <td><%= link_to 'Удалить', brand, :method => 'delete', :confirm => "Вы уверены?" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить новый брэнд', new_brand_path %>
