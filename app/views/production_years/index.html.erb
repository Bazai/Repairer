# encoding: utf-8
<h1>Список годов выпуска</h1>

<table>
  <tr>
    <th>Год</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @production_years.each do |production_year| %>
  <tr>
    <td><%= production_year.car_model.brand.name %></td>
    <td><%= production_year.car_model.name %></td>
    <td><%= production_year.year %></td>
    <td><%= link_to 'Show', production_year %></td>
    <td><%= link_to 'Edit', edit_production_year_path(production_year) %></td>
    <td><%= link_to 'Destroy', production_year, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Production year', new_production_year_path %>
