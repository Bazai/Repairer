<%= form_for(@production_year) do |f| %>
  <% if @production_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@production_year.errors.count, "error") %> prohibited this production_year from being saved:</h2>

      <ul>
      <% @production_year.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br />
    <%= f.number_field :year %>
  </div>
  <p><%= @production_year.car_model.inspect %></p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
